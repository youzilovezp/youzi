# /youzi 入门指南 🎯

> **写给完全没用过 Claude Code 的朋友**
> 不需要任何编程基础，跟着点就好。

---

## 📖 这是什么？

`/youzi` 是一个 Claude Code 的"超能力"工具。装上之后，你只要在 Claude 里输入一行话，比如：

```
/youzi 在线协作工具
```

**5 分钟后**，你就会得到一份：
- 📊 十几页精美的竞品分析报告（HTML 网页）
- 📈 包含维度对比、雷达图、价格表、市场空白
- 💡 AI 帮你想出 5 个"颠覆性产品机会"

---

## 🔧 第一步：检查你的电脑

`/youzi` 需要：

| 工具 | 怎么检查 | 没装怎么办 |
|------|---------|----------|
| **Claude Code** | 打开终端输入 `claude --version` | 去 [claude.com/download](https://claude.com/download) 下载 |
| **Python 3.8+** | 终端输入 `python3 --version` | Mac 自带；Windows 去 [python.org](https://python.org) 下载 |
| **网络** | 能打开 google.com 就行 | - |

**Mac 用户**额外需要：
- Homebrew（Mac 包管理器）：打开终端，粘贴这一行：
  ```bash
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  ```
- 看不懂？直接跳过，95% 的情况已经装好了。

---

## 📥 第二步：下载 /youzi

打开**终端**（Mac 叫"Terminal"，Windows 叫"PowerShell"或"命令提示符"）。

> 💡 不知道怎么打开终端？
> - Mac：按 `Cmd + 空格`，输入 `Terminal`，回车
> - Windows：开始菜单搜 `PowerShell`，回车

**把下面这一行整段复制粘贴到终端，回车**：

```bash
cd ~/Documents && git clone https://github.com/你的用户名/youzi.git
```

> ⚠️ **重要**：上面的 `你的用户名` 改成你实际存代码的地方。如果你不知道 `git` 是什么，先装它：
> - Mac：在终端输入 `brew install git`
> - Windows：去 [git-scm.com](https://git-scm.com/download/win) 下载

**如果上面的命令看不懂**，就直接到 [github.com/你的用户名/youzi](https://github.com) 页面，点绿色的 `Code` 按钮 → `Download ZIP`，下载后解压到任何位置。

---

## 🔌 第三步：连接到 Claude Code

把 skill 文件夹**链接**到 Claude Code 能找到的位置。在终端输入：

```bash
mkdir -p ~/.claude/skills
ln -s ~/youzi ~/.claude/skills/youzi
```

> 💡 **什么是 symlink？** 就像 Windows 的"快捷方式"或 Mac 的"替身"。改了原始文件，链接也会同步更新。

**验证是否成功**（应该能看到 SKILL.md 文件）：

```bash
ls ~/.claude/skills/youzi/SKILL.md
```

如果看到 `SKILL.md` 这个文件名 = **成功** ✅

---

## 🔧 进阶安装（强烈推荐）

`/youzi` 用了两个超能力工具来爬取数据。装上它们，分析质量会提升 5 倍：

| 工具 | 作用 | 不装会怎样 |
|------|------|---------|
| **firecrawl** | 爬网页（96% 覆盖 + 处理 JS 重度网站）| 一些竞品官网抓不到 |
| **Crawl4AI** | 开源免费、本地化、firecrawl 失败时自动接管 | 多 100MB 浏览器 |
| **Playwright** | 处理登录/交互场景 | 多 150MB 浏览器 |
| **Agent-Reach** | 视频字幕 / 社交 / 语义搜索 | 看不到产品演示视频 |

---

### 🔥 安装 firecrawl（爬网页必备）

**firecrawl 是什么**：业界最强的网页爬虫，能抓 96% 的网站（包括 JS 重度的 SPA）。

**步骤 1：注册账号拿 API key**

1. 打开浏览器，访问 [firecrawl.dev](https://firecrawl.dev)
2. 点 `Sign Up` 注册（可以用 Google 账号一键登录）
3. 登录后，左侧菜单点 `API Keys`
4. 点 `Create API Key`，命名（比如 `youzi-local`），复制保存

> 💡 **不要关闭这个页面！** 接下来要用这个 key。

**步骤 2：安装 firecrawl CLI**

打开终端，**整段复制粘贴**：

```bash
npm install -g firecrawl-cli
```

> ⚠️ 如果提示 `npm: command not found`，先装 Node.js：
> - Mac：`brew install node`（或去 [nodejs.org](https://nodejs.org) 下载）
> - Windows：去 [nodejs.org](https://nodejs.org) 下载安装包

**步骤 3：配置 API key**

把刚才复制的 key 粘到终端（**替换 `fc-xxxxx` 为你的真实 key**）：

```bash
firecrawl config -k fc-你的APIkey
```

如果不想暴露命令历史，可以：

```bash
# Mac / Linux
export FIRECRAWL_API_KEY='fc-你的APIkey'
echo "export FIRECRAWL_API_KEY='fc-你的APIkey'" >> ~/.zshrc  # 永久生效
```

**步骤 4：验证**

```bash
firecrawl --status
```

**应该看到**：
```
🔥 firecrawl cli v1.20.0
● Authenticated via FIRECRAWL_API_KEY
Credits: 1,238 / 1,000
```

看到 `Authenticated` = **成功** ✅

---

### 🔧 安装 Crawl4AI（开源免费 · 推荐）

**Crawl4AI 是什么**：开源免费的 Python 爬虫，**firecrawl 失败时自动接管**。本机跑，不消耗 API 信用。

**步骤 1：安装**

```bash
pip install crawl4ai
crawl4ai-setup  # 下载浏览器依赖（首次运行）
```

**步骤 2：验证**

```bash
python3 -c "from crawl4ai import AsyncWebCrawler; print('OK')"
```

看到 `OK` = 成功。

---

### 🎭 安装 Playwright（按需 · 处理登录场景）

**Playwright 是什么**：微软官方浏览器自动化，**唯一能处理登录/交互**。

**步骤 1：安装**

```bash
pip install playwright
playwright install chromium  # 下载 Chromium（首次约 150MB）
```

**步骤 2：验证**

```bash
python3 -c "from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True); print('OK'); b.close(); p.stop()"
```

看到 `OK` = 成功。

> **哪个必装？** 看你的需求：
> - 普通抓数据 → **firecrawl**（推荐）
> - 想省钱/隐私 → **Crawl4AI**（免费，本地）
> - 需要登录 → **Playwright**（必装）
> - **推荐全装**（3 个并行跑，分析质量提升 5 倍）

---

### 🎬 安装 Agent-Reach（视频/社交数据，可选）

**Agent-Reach 是什么**：能拉取 YouTube 字幕、小红书、GitHub 仓库、X/Twitter 公开内容。如果你分析的是 ToC 产品（视频、社交），强烈建议装。

**步骤 1：git clone 项目**

```bash
git clone https://github.com/Panniantong/Agent-Reach.git ~/.agent-reach
```

**步骤 2：安装**

```bash
cd ~/.agent-reach
pip install -e .
```

> ⚠️ 如果 `pip` 报错，试 `pip3 install -e .` 或 `python3 -m pip install -e .`

**步骤 3：让 Agent-Reach 接入 Claude Code**

在 Claude Code 里说：

```
请安装并启用 Agent-Reach（github.com/Panniantong/Agent-Reach）
```

Claude 会自动完成 MCP 配置。

**步骤 4：验证**

```bash
agent-reach doctor
```

**应该看到**：
```
✅ 任意网页（Jina Reader）
✅ YouTube 字幕（yt-dlp）
⚠️ Twitter / Reddit（需要登录）
...
```

看到 ✅ = **成功** ✅

---

### 🚀 第六步：第一次使用（升级版）

打开 Claude Code，输入：

```
/youzi 你想做的主题
```

**示例**：

```
/youzi 在线协作工具
/youzi AI 编程助手
/youzi 短视频剪辑软件
/youzi 跨境电商
```

**接下来会发生什么**：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⏳ 正在搜索 6 个搜索引擎...
⏳ 正在爬取 6 个竞品官网...
⏳ 正在分析 13 个维度...
⏳ 正在生成 HTML 报告...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 报告已生成！
📂 ~/youzi-out/在线协作工具-2026-08-17/report.html

要打开吗？(y/n)
```

**输入 `y` 回车**，浏览器会自动打开报告。

---

## 📊 报告长什么样？

报告有 8 大模块：

```
┌─────────────────────────────────────┐
│ 🎯 [主题] · 竞争格局与机会图谱        │
├─────────────────────────────────────┤
│ 01 市场细分                          │
│ 02 竞品画像（每家详情 + 访问链接）    │
│ 03 六维雷达对比                      │
│ 04 功能全集（真实截图 + 63 个功能）   │
│ 05 功能矩阵                          │
│ 06 市场空白                          │
│ 07 颠覆性产品机会（5 个 idea）       │
│ 08 定价对照                          │
└─────────────────────────────────────┘
```

**右上角有个 🌗 按钮** — 点击切换浅色 / 深色模式。

---

## 💡 常用命令速查

### 基础用法

```bash
/youzi 主题
```

### 限定地区

```bash
/youzi 短视频工具 --region cn       # 只看中国市场
/youzi SaaS 平台 --region us        # 只看美国市场
/youzi 电商 --region global         # 全球（默认）
```

### 指定数量

```bash
/youzi AI 编程 --count 5           # 只爬 5 个竞品（默认 9，更快）
/youzi AI 编程 --count 15          # 爬 15 个（更全，更慢）
```

### 指定深度

```bash
/youzi CRM --depth shallow         # 默认：每个竞品 1 页
/youzi CRM --depth deep            # 每个竞品 5+ 页（更详细）
```

### 强制包含 / 排除

```bash
/youzi 协作工具 --include "Slack,Notion"           # 一定要分析 Slack 和 Notion
/youzi 协作工具 --exclude "Worktile"                # 不要分析 Worktile
```

### 多个选项组合

```bash
/youzi 跨境电商 --region cn --count 8 --depth deep --focus "移动端"
```

---

## 📂 报告文件在哪？

报告生成后存在这里：

```
📁 ~/youzi-out/
   📁 在线协作工具-2026-08-17/    ← 每个主题一个文件夹
      📄 report.html                 ← 你的报告（直接打开）
      📁 02-raw/                     ← 原始爬取数据（可删）
      📄 03-analysis.json            ← 中间数据（可删）
```

**Mac 打开文件夹**：
```bash
open ~/youzi-out/
```

**Windows 打开文件夹**：
- 在文件管理器地址栏输入 `%USERPROFILE%\youzi-out`

---

## 🔄 第二次以后

**不需要**重装任何东西。直接打开 Claude Code，输入：

```
/youzi 下一个主题
```

就这么简单。

---

## 🆕 更新到最新版

```bash
cd ~/youzi
git pull
```

如果用 ZIP 下载的，重新下载覆盖即可。

---

## 🗑️ 卸载

把 symlink 删掉：

```bash
unlink ~/.claude/skills/youzi
```

可选：删除下载的代码：

```bash
rm -rf ~/youzi
```

可选：清理历史报告：

```bash
rm -rf ~/youzi-out
```

---

## ❓ 我卡住了怎么办？

### 问题 1：终端里输入 `claude` 提示"找不到命令"

**原因**：Claude Code 没装好，或者没加到 PATH。

**解决**：
1. 重新安装 [claude.com/download](https://claude.com/download)
2. 重启终端
3. 再试一次

### 问题 2：报告生成一半报错

**症状**：类似 `❌ firecrawl error: ...` 或 `⚠️ 解析失败`

**解决**：
1. 看错误信息是什么
2. 把错误信息复制给你的 Claude（或直接发给我）
3. 通常是网络问题，重试一次就好

### 问题 3：报告里某些竞品数据是空的

**症状**：比如 WATI 卡片里没有截图

**原因**：firecrawl 抓取的网站有反爬
**解决**：暂时无解，等 firecrawl 优化。可以试 `--refresh` 重抓。

### 问题 4：浏览器打开报告是乱码

**症状**：报告显示 `{{topic}}` 或 `{% for %}`

**原因**：模板没有渲染完成
**解决**：重新运行 `/youzi 你的主题`，等 30 秒再试。

### 问题 5：报告打开后内容很多，看不懂

**这很正常**！报告是给"产品经理"看的专业材料。不要试图一次性全读懂，重点看：

1. **02 竞品画像** — 每家竞品的"强项"和"弱项"
2. **07 颠覆性产品机会** — AI 给你的 5 个想法
3. **06 市场空白** — 没人解决的问题

如果还是不懂，把报告丢给 Claude Code，让它用大白话给你解读！

---

## 🎁 进阶玩法

### 批量分析

```bash
#!/bin/bash
# 保存为 ~/analyze.sh
for topic in "在线协作工具" "AI 编程助手" "短视频剪辑"; do
  echo "分析: $topic"
  # 在 Claude Code 里输入 /youzi $topic
done
```

### 导出到 Notion

```bash
# 把竞品列表导出成 Notion 表格
cat ~/youzi-out/在线协作工具-2026-08-17/03-analysis.json | jq '.competitors[].name'
```

### 改报告样式

打开 `youzi/templates/report.html` 直接修改 HTML。
改完不需要重跑，数据会自动套用新样式。

---

## 🆘 寻求帮助

1. **看错误信息** — 99% 的错误都有线索
2. **复制整个错误** 发给 Claude Code 问："这个报错是什么意思？"
3. **查看 MANIFEST.md** — 项目根目录有更详细的技术参考
4. **GitHub Issues** — 提交 bug 或功能建议

---

## 🎓 常用词速查

| 术语 | 通俗解释 |
|------|---------|
| **Skill** | Claude Code 的"插件"，扩展 Claude 的能力 |
| **Symlink** | 文件夹的"快捷方式"（修改原始 → 链接同步更新）|
| **Web Scraping** | 自动从网页抓取内容 |
| **API** | 别人的"数据服务员"，可以问它要数据 |
| **CLI** | 命令行界面（在终端里输入文字操作）|
| **JSON** | 一种数据格式（看起来像 `{"key": "value"}`）|
| **HTML** | 网页的源代码 |
| **Regex** | 文字搜索模式（用来查找特定格式）|

---

**祝你玩得开心！** 🎉

如果这份指南有问题或不清楚的地方，告诉我具体哪里卡住，我帮你改。
