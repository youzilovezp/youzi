# /youzi 极速入门 ⚡

> **5 分钟从 0 到第一份报告**
> 不需要任何编程基础，跟着点就好。

---

## 0️⃣ 前置（5 分钟，只做一次）

打开**终端**（Mac：`Cmd+空格` → 搜 `Terminal`），**整段复制粘贴**：

```bash
# 装 Python（系统工具）
python3 --version || brew install python3

# 装 Node.js（firecrawl 需要）
node --version || brew install node

# 装 Claude Code（去 https://claude.com/download 下载）

# 装 /youzi
mkdir -p ~/.claude/skills
ln -s /path/to/youzi ~/.claude/skills/youzi
```

验证（应该都看到版本号）：

```bash
python3 --version    # Python 3.8+
node --version      # v18+
claude --version    # Claude Code v1.0+
```

---

## 1️⃣ 装爬虫工具（推荐 · 2 分钟）

`/youzi` 装了 3 个爬虫工具**并行工作**，覆盖率从 50% 提升到 99%。

```bash
# 必备：firecrawl（最强商业爬虫，96% 覆盖）
# 1. 去 https://firecrawl.dev 注册 → 拿 API key（以 fc- 开头）
# 2. 配置：
npm install -g firecrawl-cli
firecrawl config -k fc-你的APIkey

# 推荐：Crawl4AI（开源免费，本地化）
pip install crawl4ai

# 按需：Playwright（处理登录场景）
pip install playwright && playwright install chromium
```

---

## 2️⃣ 第一次跑（30 秒）

打开 Claude Code，输入：

```
/youzi 在线协作工具
```

**等 3-5 分钟**。完成后报告自动打开。

---

## 3️⃣ 看报告

报告长这样：

```
┌─────────────────────────────────────┐
│ 01 市场细分                          │
│ 02 竞品画像（含访问链接 + 6 张截图）  │
│ 03 六维雷达对比                      │
│ 04 功能全集（63 项功能）              │
│ 05 功能矩阵                          │
│ 06 市场空白（5 个机会）              │
│ 07 颠覆性产品机会（5 个 idea）       │
│ 08 定价对照                          │
└─────────────────────────────────────┘
```

右上角 🌗 按钮 = 切换深色模式。

---

## 4️⃣ 下次跑

```
/youzi 你想做的主题
```

就这么简单。

---

## 🆘 5 分钟排错

| 问题 | 一行解决 |
|------|---------|
| `claude: command not found` | 重装 Claude Code |
| `firecrawl: command not found` | `npm install -g firecrawl-cli` |
| 报告是乱码（`{{xxx}}`）| 重新跑 `/youzi 主题` |
| 某些竞品数据空白 | 装 `pip install crawl4ai playwright && playwright install chromium` |
| 报错 `FIRECRAWL_API_KEY not found` | `firecrawl config -k fc-xxx` |
| 想用中文社交/视频数据 | `git clone https://github.com/mcgrapeng/Agent-Reach.git ~/.agent-reach && cd ~/.agent-reach && pip install -e .` |
| 还是卡住 | 在 Claude Code 里说"我卡住了，把错误信息给我" |

---

## 📚 想深入了解？

| 想看什么 | 跳到 |
|---------|------|
| 完整安装步骤 | `MANUAL.md` |
| 纯小白版（5 分钟）| `小白入门.md` |
| 爬虫技术对比 | `references/scraping-tools.md` |
| 13 字段提取 | `references/analysis-framework.md` |
| 模板/源码 | `templates/` `adapters/` `render.py` |
| 示例报告 | `examples/sample-report.html` |

---

**5 分钟搞定，享受 `/youzi` 吧！** 🎉
