# /youzi · 竞品颠覆性分析技能 — 使用手册

> 一句话：**`/youzi <主题>` → 自动挖顶尖竞品 → 深度分析 → 颠覆性机会清单 → 精美 HTML 报告**
>
> 本手册涵盖：安装、卸载、更新、使用、进阶技巧、故障排查。

---

## 📑 目录

1. [快速开始](#1-快速开始)
2. [安装](#2-安装)
3. [使用](#3-使用)
4. [配置（可选）](#4-配置可选)
5. [更新](#5-更新)
6. [卸载](#6-卸载)
7. [进阶用法](#7-进阶用法)
8. [输出物说明](#8-输出物说明)
9. [常见问题 FAQ](#9-常见问题-faq)
10. [故障排查](#10-故障排查)

---

## 1. 快速开始

如果你只想立刻用，跳过其他章节，看这里：

```bash
# 1. 安装 skill（一次性）
mkdir -p ~/.claude/skills
cp -R /path/to/youzi ~/.claude/skills/youzi

# 2. 在 Claude Code 里直接输入
/youzi 在线协作工具

# 3. 几分钟后报告生成在 ~/youzi-out/在线协作工具-2026-08-15/report.html
open ~/youzi-out/在线协作工具-2026-08-15/report.html
```

---

## 2. 安装

### 2.1 最小安装（只要 skill 本体）

```bash
# 方式 A：复制整个目录
cp -R ~/.claude/skills/youzi ~/.claude/skills/

# 方式 B：用 symlink（推荐，源文件更新即时生效）
ln -s ~/youzi ~/.claude/skills/youzi

# 验证
ls -la ~/.claude/skills/youzi/SKILL.md
# 应该看到 SKILL.md 存在
```

### 2.2 完整安装（skill + 增强 MCP 工具链 — 强烈推荐）

`/youzi` 默认用 Claude Code 内置的 `WebSearch` + `WebFetch` 工具爬数据，但加装这两个工具能让分析质量提升 5 倍：

| 工具 | 来源 | 作用 | 必装？ |
|------|------|------|------|
| **firecrawl** | [github.com/mcgrapeng/firecrawl](https://github.com/mcgrapeng/firecrawl) | 96% 网页覆盖 + JS 重度处理 | ✅ 必装 |
| **Agent-Reach** | [github.com/mcgrapeng/Agent-Reach](https://github.com/mcgrapeng/Agent-Reach) | YouTube 字幕 / 社交 / 语义搜索 | ⭐ 推荐 |

#### 2.2.1 安装 firecrawl（4 步）

**步骤 1：注册账号拿 API key**

1. 访问 [firecrawl.dev](https://firecrawl.dev)，注册账号（支持 Google 一键登录）
2. 控制台 → `API Keys` → `Create API Key`
3. 命名（比如 `youzi-local`），**复制保存**（以 `fc-` 开头）

> 💡 **不要关闭页面！** 接下来要用这个 key。

**步骤 2：安装 CLI**

```bash
# 前置：需要 Node.js 18+
npm install -g firecrawl-cli
```

> ⚠️ 如果 `npm: command not found`，先装 Node.js：
> - Mac：`brew install node` 或去 [nodejs.org](https://nodejs.org) 下载
> - Windows：去 [nodejs.org](https://nodejs.org) 下载安装包

**步骤 3：配置 API key**

```bash
firecrawl config -k fc-你的APIkey
```

或永久生效（写入 shell 配置）：

```bash
# Mac / Linux
export FIRECRAWL_API_KEY='fc-你的APIkey'
echo "export FIRECRAWL_API_KEY='fc-你的APIkey'" >> ~/.zshrc
```

**步骤 4：验证**

```bash
firecrawl --status
```

**应该看到**：
```
🔥 firecrawl cli v1.20.0
● Authenticated via FIRECRAWL_API_KEY
Credits: 1,238 / 1,000
```

看到 `Authenticated` = **成功** ✅

#### 2.2.2 安装 Agent-Reach（4 步）

**步骤 1：克隆项目**

```bash
git clone https://github.com/mcgrapeng/Agent-Reach.git ~/.agent-reach
```

**步骤 2：安装**

```bash
cd ~/.agent-reach
pip install -e .
```

> ⚠️ 如果 `pip` 报错，试 `pip3 install -e .` 或 `python3 -m pip install -e .`

**步骤 3：让 Agent-Reach 接入 Claude Code**

在 Claude Code 里说：

```
请安装并启用 Agent-Reach（github.com/mcgrapeng/Agent-Reach）
```

Claude 会自动完成 MCP 配置。

**步骤 4：验证**

```bash
agent-reach doctor
```

**应该看到**：
```
✅ 任意网页（Jina Reader）
✅ YouTube 字幕（yt-dlp）
⚠️ Twitter / Reddit（需要登录）
```

看到 ✅ = **成功** ✅

#### 2.2.3 验证两个工具都装好

所有装好后，在 Claude Code 里输入 `/mcp`，应该看到：

```
✓ firecrawl     (config: fc-xxx...)
✓ agent-reach   (8/15 channels ready)
```

如果两个 ✓ 都有 = **完美配置** ✅

#### 2.2.4 安装 Crawl4AI（推荐 · 开源免费 fallback）

`/youzi` 默认**并行**调用 3 个爬虫（firecrawl + Crawl4AI + Playwright），Crawl4AI 失败时自动接管。

```bash
pip install crawl4ai
crawl4ai-setup  # 安装 Playwright 浏览器依赖
```

**验证**：
```bash
python3 -c "from crawl4ai import AsyncWebCrawler; print('✓ Crawl4AI 可用')"
```

#### 2.2.5 安装 Playwright（按需 · 处理登录场景）

```bash
pip install playwright
playwright install chromium
```

**验证**：
```bash
python3 -c "from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True); print('✓ Chromium 启动'); b.close(); p.stop()"
```

**源码**：[github.com/mcgrapeng/firecrawl](https://github.com/mcgrapeng/firecrawl) · [github.com/mcgrapeng/Agent-Reach](https://github.com/mcgrapeng/Agent-Reach) · [github.com/mcgrapeng/Agent-Reach](https://github.com/mcgrapeng/Agent-Reach)

### 2.3 系统要求

| 项目 | 要求 | 用途 |
|------|------|------|
| Claude Code | v1.0+ | 必装 |
| Python | 3.8+ | 渲染 HTML + Crawl4AI + Playwright（零依赖渲染器） |
| Node.js | 18+ | firecrawl-cli（可选，但推荐） |
| 操作系统 | macOS / Linux / Windows 均可 | - |
| 磁盘空间 | < 10 MB（skill 本体） | 截图另算 |

### 2.4 安装位置说明

```
~/.claude/skills/youzi/        # Skill 安装位置（Claude Code 读取这里）
  ├── SKILL.md                 # ⚠️ 必须有，Claude 靠这个识别 skill
  ├── render.py                # HTML 渲染器
  ├── templates/report.html    # HTML 模板
  ├── references/              # 详细文档
  └── examples/                # 示例数据
```

---

## 3. 使用

### 3.1 基础用法

```
/youzi <主题>
```

**示例：**

```
/youzi 在线协作工具
/youzi AI 编程助手
/youzi 视频剪辑软件
/youzi 团队 IM 工具
/youzi 跨境电商平台
```

### 3.2 完整参数

```
/youzi <主题> [选项]

选项：
  --depth shallow|deep        # 爬取深度（默认 shallow）
  --count N                    # 竞品数量（默认 9）
  --region cn|us|global        # 限定区域（默认 global）
  --focus "文本"               # 分析焦点（如 "中小企业 / 移动端"）
  --include "Slack,钉钉"       # 强制包含指定竞品
  --exclude "Worktile"         # 排除指定竞品
  --refresh                    # 强制重新爬取（忽略缓存）
  --no-viz                     # 只输出 JSON，不渲染 HTML
```

**示例：**

```bash
# 深度分析 + 12 个竞品
/youzi AI 编程助手 --depth deep --count 12

# 限定中国市场 + 移动端焦点
/youzi 短视频工具 --region cn --focus "移动端 抖音生态"

/youzi 跨境电商 --include "Shopee,Temu,SHEIN"

/youzi 低代码平台 --exclude "OutSystems"

/youzi AI 写作 --depth deep --refresh
```

### 3.3 输出位置

默认输出到：

```
~/youzi-out/<主题>-<日期>/
├── 01-competitors-list.json   # 竞品候选清单
├── 02-raw/                    # 原始爬取数据
│   ├── <name>.md              # 每个竞品的网页内容
│   ├── <name>-visual.md       # 视觉分析（可选）
│   ├── <name>-code.md         # 代码仓库分析（可选）
│   └── <name>-social.md       # 社交信号（可选）
├── 03-analysis.json           # 结构化分析结果
└── report.html                # ⭐ 最终报告（70KB 单文件）
```

**直接打开报告：**

```bash
# macOS
open ~/youzi-out/<主题>-<日期>/report.html

# Linux
xdg-open ~/youzi-out/<主题>-<日期>/report.html

# Windows
start ~/youzi-out/<主题>-<日期>/report.html
```

---

## 4. 配置（可选）

skill 没有强制配置，但你可以定制输出：

### 4.1 修改报告主题

```bash
# 浅色 / 深色 / 跟随系统（默认）
# 在 report.html 右上角有切换按钮，选择会记住到 localStorage
```

### 4.2 修改默认竞品数量

编辑 `~/.claude/skills/youzi/SKILL.md`，找到 `--count N`（默认 9），改成你想要的数字。

### 4.3 添加自定义评分维度

编辑 `~/.claude/skills/youzi/render.py`：

```python
# 找到 SCORE_DIMS，添加新维度
SCORE_DIMS = [
    ("feature_richness", "功能丰富度"),
    ("ux",               "用户体验"),
    ("pricing_value",    "定价性价比"),
    ("integration",      "生态集成"),
    ("ai_capability",    "AI 能力"),
    ("momentum",         "增长势头"),
    ("security",         "安全合规"),   # ← 你加的
]
```

然后在数据 JSON 里加 `"scores": { ..., "security": 8 }`。

### 4.4 修改配色

编辑 `~/.claude/skills/youzi/templates/report.html`，找到 `:root` 里的 `--accent` 等变量。

---

## 5. 更新

### 5.1 更新 skill 本体

```bash
# 如果用 symlink 安装（推荐）
cd ~/youzi
git pull  # 或手动替换文件

# 如果用 cp 安装
cd ~/.claude/skills/youzi
git pull  # 或从新版本覆盖

# 验证版本
head -5 ~/.claude/skills/youzi/SKILL.md
```

### 5.2 更新 firecrawl

```bash
npm update -g firecrawl-cli
firecrawl --version  # 验证
```

源码更新：[github.com/mcgrapeng/firecrawl](https://github.com/mcgrapeng/firecrawl)

### 5.3 更新 Agent-Reach

```bash
# 在 Claude Code 里说："请更新 Agent-Reach 到最新版"
# 或：
cd ~/.agent-reach
git pull
# 然后重启 Claude Code
```

### 5.4 查看 changelog

每个版本的变更记录在：

```
~/.claude/skills/youzi/CHANGELOG.md
```

---

## 6. 卸载

### 6.1 卸载 skill 本体

```bash
# 方式 A：直接删除目录
rm -rf ~/.claude/skills/youzi

# 方式 B：如果用 symlink
unlink ~/.claude/skills/youzi

# 验证（应该没输出了）
ls ~/.claude/skills/youzi
```

### 6.2 卸载 firecrawl

```bash
npm uninstall -g firecrawl-cli
# 或手动从 ~/.claude/ 的 MCP 配置里删掉 firecrawl 段
```

### 6.3 卸载 Agent-Reach

```bash
rm -rf ~/.agent-reach
# 然后从 Claude Code MCP 配置里删 agent-reach 段
```

### 6.4 清理输出文件（可选）

```bash
# 删除所有生成的报告
rm -rf ~/youzi-out/

# 只保留最近 30 天
find ~/youzi-out/ -maxdepth 1 -type d -mtime +30 -exec rm -rf {} \;
```

---

## 7. 进阶用法

### 7.1 重渲染（不改数据）

如果你只想改报告样式 / 加新维度，**不用重新爬**：

```bash
# 直接用已有 JSON 重渲染
python3 ~/.claude/skills/youzi/render.py \
  --input ~/youzi-out/AI编程助手-2026-08-15/03-analysis.json \
  --output ~/youzi-out/AI编程助手-2026-08-15/report-v2.html
```

### 7.2 批量分析多个主题

```bash
#!/bin/bash
# batch-analyze.sh
for topic in "在线协作工具" "AI 编程助手" "视频剪辑工具"; do
  echo "=== Analyzing: $topic ==="
  # 在 Claude Code 里调一次 /youzi $topic
done
```

### 7.3 与其他工具组合

```bash
# /youzi 跑完后，把 JSON 转给 Notion
cat ~/youzi-out/<topic>/03-analysis.json | jq '.opportunities' | \
  curl -X POST https://api.notion.com/v1/pages -H "Authorization: Bearer $NOTION_TOKEN"

# 把 HTML 报告上传到 Confluence
curl -X POST -u user:pass \
  --data-binary @~/youzi-out/<topic>/report.html \
  https://your-confluence/wiki/rest/api/content
```

### 7.4 自定义分析维度

在 `references/analysis-framework.md` 里改 13 字段定义。Claude 会按新字段提取。

### 7.5 导出 JSON 数据

```bash
# 拿 JSON 喂给其他 BI 工具
cat ~/youzi-out/<topic>/03-analysis.json | \
  python3 -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d['competitors'], ensure_ascii=False, indent=2))"
```

### 7.6 在 CI/CD 里定期跑

```yaml
# .github/workflows/weekly-competitive.yml
name: Weekly Competitive Analysis
on:
  schedule: [{cron: '0 9 * * MON'}]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run /youzi
        run: |
          # 调 Claude Code CLI
          claude code --skill youzi --prompt "分析：跨境电商 SaaS"
```

---

## 8. 输出物说明

### 8.1 报告 HTML 结构（9 大模块）

```
1. Hero         - 标题 + 元信息
2. TOC          - 目录锚点
3. 执行摘要     - TL;DR 200 字
4. 市场细分     - 玩家分群
5. 竞品画像     - 每个竞品的卡片（×N）
6. 六维雷达     - 雷达图对比
7. 功能重叠     - 热力图
8. 定价对比     - 价格 + 性价比
9. 市场空白     - 所有竞品都没做好的痛点
10. 颠覆性机会  - 灵感 + 用户 + 差异化 + 验证 + 护城河
11. 方法论      - 流程说明
```

### 8.2 报告交互能力

- 🌗 **右上角按钮** — 切换浅色 / 深色 / 跟随系统
- 📑 **目录点击** — 锚点跳转
- 🎯 **雷达图例** — 点击显隐某个竞品
- 🖨️ **打印** — Ctrl+P / Cmd+P 直接打印（CSS 优化过）

### 8.3 JSON 数据结构

见 `references/analysis-framework.md` 末尾的完整 schema。

---

## 9. 常见问题 FAQ

### Q1：跑一次要多久？

| 模式 | 时间 |
|------|------|
| shallow + 9 竞品（默认） | 3-5 分钟 |
| deep + 12 竞品 | 8-15 分钟 |
| shallow + 3 竞品 | 1-2 分钟 |

时间主要花在 WebFetch / firecrawl 爬网页 + LLM 分析。

### Q2：报告是单文件吗？能离线看吗？

**是的。** 报告 68KB 单文件，CSS/JS/SVG 全部 inline，**零外部依赖**。可以邮件发送、上传网盘、放进 USB，断网也能看。

### Q3：能分析中文竞品吗？

**可以。** Skill 默认就是中文友好：
- 字体栈优先 PingFang SC / 微软雅黑
- 搜索引擎自动用中文查询
- 推荐装 **Agent-Reach** 解锁 X/小红书/微博/B站等中文社交信号

### Q4：数据是实时抓的吗？会不会过时？

**每次跑都是实时抓的。** 建议：
- 高频赛道（AI、加密）每月刷一次
- 稳定赛道（SaaS、电商）每季度刷一次

`--refresh` 强制重爬；不加则用 `02-raw/` 缓存。

### Q5：能跑私有数据吗？会上传到云吗？

**不会。** skill 本体只调本地工具（WebSearch / WebFetch / MCP）。所有分析在本地 Claude 里完成，HTML 写到本地磁盘。

### Q6：生成的报告能改吗？

**能。** 三种方式：
1. **手动改 HTML** — 直接编辑 `report.html`
2. **改模板** — 编辑 `templates/report.html`，重跑 `render.py`
3. **改数据** — 编辑 `03-analysis.json`，重跑 `render.py`

### Q7：竞品数量有上限吗？

**理论无上限**，但建议 5-15 个。
- 太少（< 5）：覆盖不全，颠覆性机会少
- 太多（> 20）：LLM 分析质量下降，报告冗长

### Q8：怎么保证分析质量？

skill 强制要求 **每条 strengths / weaknesses 必须带 URL 证据**（来自抓到的网页 / 评测）。如果证据不足，会标"无足够证据"，不瞎编。

---

## 10. 故障排查

### 问题 1：`/youzi` 命令不识别

**症状**：在 Claude Code 输入 `/youzi XXX`，提示"未找到命令"。

**排查**：

```bash
# 1. 检查 skill 目录是否存在
ls -la ~/.claude/skills/youzi/SKILL.md

# 2. 如果不存在，重新安装
ln -s ~/youzi ~/.claude/skills/youzi

# 3. 重启 Claude Code
```

### 问题 2：报告生成但内容空白

**症状**：HTML 生成成功（70KB），但卡片里 strengths / weaknesses 是空的。

**原因**：通常是 WebFetch 抓到的网页是 JS 重度渲染（SPA），只拿到空 HTML。

**解决**：

```bash
# 装 firecrawl（处理 96% SPA）
npx -y firecrawl-cli@latest init

# 或在 SKILL.md 顶部把 web-reader MCP 加入 allowed-tools（已加）
```

### 问题 3：爬取超时

**症状**：`02-raw/` 里某些竞品的 md 文件大小为 0。

**解决**：

```bash
# 1. 重跑（自动重试失败的）
/youzi <主题> --refresh

# 2. 如果还是失败，单独跑深度模式
/youzi <主题> --depth deep

# 3. 终极方案：手动指定竞品
/youzi <主题> --include "已确认能爬到的竞品"
```

### 问题 4：firecrawl 报 API key 错

```bash
# 去 https://firecrawl.dev 免费注册
# 拿到 API key 后：
npx -y firecrawl-cli@latest config set api_key fc-xxxxx
```

### 问题 5：报告样式错乱

```bash
# 1. 清浏览器缓存
# 2. 检查 HTML 完整性
wc -c ~/youzi-out/<topic>/report.html
# 应该 ~68KB

# 3. 如果 < 50KB，说明渲染器出问题
python3 ~/.claude/skills/youzi/render.py \
  --input ~/youzi-out/<topic>/03-analysis.json \
  --output /tmp/test.html --no-check
# 看错误信息
```

### 问题 6：磁盘被报告占满

```bash
# 清理旧报告
find ~/youzi-out/ -maxdepth 1 -type d -mtime +30 -exec rm -rf {} \;

# 或一次性全删
rm -rf ~/youzi-out/
```

### 问题 7：中文乱码

```bash
# 1. 确保 terminal locale 是 UTF-8
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8

# 2. 确保 HTML 文件本身是 UTF-8（默认就是）
file ~/youzi-out/<topic>/report.html
# 应该输出 "UTF-8 Unicode text"

# 3. 浏览器强制 UTF-8：Chrome DevTools → Network → 勾选 "Disable cache"
```

---

## 📞 获取帮助

| 渠道 | 链接 |
|------|------|
| 项目地址 | `~/youzi` |
| 示例数据 | `~/.claude/skills/youzi/examples/` |
| 分析框架 | `~/.claude/skills/youzi/references/analysis-framework.md` |
| 爬取策略 | `~/.claude/skills/youzi/references/crawl-strategy.md` |

---

**版本**：v2.0 · 2026-08-15 · 由 `/youzi` skill 自动生成
