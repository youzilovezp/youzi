# 📦 安装说明

> 只讲安装 / 更新 / 卸载，全程约 3 分钟。
> 怎么做竞品分析，看 [📖 使用手册.md](使用手册.md)。

---

## 这个工具能装到哪些 AI 工具里

四个都支持，装几个由你决定：

| AI 工具 | 官网 |
| --- | --- |
| Claude Code | [claude.com/download](https://claude.com/download) |
| opencode | [opencode.ai](https://opencode.ai) |
| Codex | [openai.com/codex](https://openai.com/codex) |
| EasyCode | [easycode.dev](https://easycode.dev) |

装好哪个，安装脚本就会自动发现哪个，**不需要你做任何选择**。

> ⚠️ 目前支持 macOS / Linux。Windows 用户请先装 [WSL](https://learn.microsoft.com/windows/wsl/install)，在 WSL 里从第 1 步开始。

---

## 第 1 步：装好基础工具

先逐行检查下面 2 样。**每条命令都复制到「终端」里按回车**。

### 1.1 检查 Python

```bash
python3 --version
```

- 显示 `Python 3.8` 或更高 → ✅ 通过
- 报错或版本低 → 去 [python.org](https://python.org) 下载安装（Mac 也可 `brew install python3`）

### 1.2 检查 Git

```bash
git --version
```

- 显示版本号 → ✅ 通过
- 报错 → Mac 自带不用装；Linux 运行 `sudo apt install git`

> 💡 只要 AI 工具（Claude Code / opencode / Codex / EasyCode）里**任意装好了一个**，就可以继续下一步。

---

## 第 2 步：下载本工具

```bash
git clone https://github.com/your-org/youzi.git ~/youzi
```

（仓库地址换成实际的；已经下载到本地的，跳过这步。）

---

## 第 3 步：进入目录

```bash
cd ~/youzi
```

---

## 第 4 步：安装（skill + 引擎依赖一次装齐）

```bash
./install.sh install
```

看到 `✅ 已创建 youzi` 和最后 `✅ 引擎依赖完成` 就是装好了。这一条命令会做两件事：

**先装 skill** —— 自动装到所有已检测到的 AI 工具里：

| AI 工具 | 安装位置 |
| --- | --- |
| Claude Code | `~/.claude/skills/youzi/` |
| opencode | `~/.config/opencode/skills/youzi/` + `~/.config/opencode/command/youzi.md`（`/` 补全入口） |
| Codex | `~/.codex/skills/youzi/` |
| EasyCode | `~/.easycode/skills/youzi/` |

**再自动装引擎依赖**（缺哪个装哪个，已装的不动）：

```bash
python3 -m pip install jinja2 playwright trafilatura newspaper3k lxml_html_clean
python3 -m playwright install chromium    # 已装会秒过
```

常用变体：

```bash
./install.sh install --platform claude    # 只装 Claude Code（其余平台同理）
./install.sh install --no-deps            # 只装 skill，跳过引擎依赖
./install.sh deps                         # 单独装/补装引擎依赖（可重复跑）
```

> 💡 默认**符号链接**安装——改源码即时生效。不便软链（如装到别的机器）用 `--mode copy` 完整复制。

---

## 第 5 步：引擎说明与手动兜底

`/youzi` 用 **5 引擎白名单**按页面类型智能路由，第 4 步已自动装好。**一个都不装也能跑**（降级到 WebSearch），但装齐抓取更稳。

| # | 引擎 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | playwright | 🆓 开源 | JS 页主力（定价/首页），唯一能登录交互；需 chromium |
| 2 | trafilatura | 🆓 开源 | 静态正文主力（docs/about/changelog） |
| 3 | newspaper3k | 🆓 开源 | 文章型（blog/customer/testimonials） |
| 4 | jina | 🆓 免 key | 第三方渲染，交叉验证第二票，无需安装 |
| 5 | firecrawl | 🔒 商业 | 最强；检测到 `FIRECRAWL_API_KEY` 自动启用，无 key 不尝试 |

自动安装失败时（如系统 Python 受 PEP 668 保护），手动执行：

```bash
python3 -m pip install jinja2 playwright trafilatura newspaper3k lxml_html_clean
python3 -m playwright install chromium

# 可选：firecrawl（免费 500 页/月，https://firecrawl.dev 注册）
export FIRECRAWL_API_KEY='fc-你的APIkey'
```

### 验证引擎

```bash
cd ~/youzi && ./install.sh status     # 环境检查区看引擎状态
python3 -c "from adapters import list_scrapers; [print(f'  {\"✓\" if ok else \"✗\"} {n}') for n, ok in list_scrapers().items()]"
```

期望 4 行 ✓（firecrawl 配 key 后变 5 行）。

---

## 第 6 步：重启 AI 工具

**这一步必须做**，否则 AI 工具识别不到新技能。

- Claude Code：按 `Cmd + Q`（Mac）完全退出，再重新打开
- opencode：关掉终端窗口，重新开一个
- Codex / EasyCode：退出后重新打开

---

## 第 7 步：验证

```bash
cd ~/youzi && ./install.sh status
```

每个平台都显示 `SKILL.md：✅`、环境检查里 `jinja2: ✅` 就成功了。

再打开你的 AI 工具，输入 `/youzi`，跳出自动补全 = 完全成功。

（可选）跑一次渲染冒烟测试：

```bash
python3 scripts/render_smoke.py
```

期望输出 `✓ Rendered: /tmp/pre-commit-render-smoke.html` + 自检全过。

---

## 更新

```bash
cd ~/youzi
git pull
./install.sh update
./install.sh deps    # 可选：补装/升级引擎依赖（已装的不动）
```

- **link 模式**（默认）：源码 `git pull` 后即时生效，`update` 只是检查链接指向
- **copy 模式**：必须跑 `update` 同步新文件

然后按第 6 步重启 AI 工具。已生成的报告不受影响。

---

## 卸载

```bash
cd ~/youzi
./install.sh uninstall
```

想清干净（可选）：

```bash
pip uninstall playwright trafilatura newspaper3k lxml_html_clean   # 引擎
rm -rf ~/youzi-out/                                               # 历史报告
rm -rf ~/youzi                                                    # 源码
```

---

## 常见问题

| 问题 | 解决 |
| --- | --- |
| `python: command not found` | 用 `python3`（Mac 自带）；仍没有按 1.1 节安装 |
| 提示缺 jinja2 | `pip3 install jinja2` |
| 装完不识别 `/youzi` | 按第 6 步**完全重启** AI 工具；还不行跑 `./install.sh status` 检查 |
| 提示「目标已存在」 | 之前装过，按提示输 `y` 覆盖 |
| 没检测到我的 AI 工具 | `./install.sh install --platform <平台名>` 强制指定 |
| `pip install playwright` 装浏览器失败 | 先 `python3 -m playwright install chromium`；下载慢见下一行 |
| `playwright install chromium` 下载很慢 | `export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright` |
| 设了 `FIRECRAWL_API_KEY` 还是提示无 key | 永久生效：`echo 'export FIRECRAWL_API_KEY=fc-xxx' >> ~/.zshrc && source ~/.zshrc`，重开终端 |
| Windows 用户 | 先装 [WSL](https://learn.microsoft.com/windows/wsl/install)：PowerShell 里运行 `wsl --install`，重启后在 WSL 里从第 1 步开始 |

---

**下一步**：打开你的 AI 工具，输入 `/youzi 在线协作工具`，跟着 [📖 使用手册.md](使用手册.md) 走。
