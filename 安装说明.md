# 📦 /youzi 安装说明

> **目标**: 把 `/youzi` 装到你的 Claude Code,从 0 到能跑通 `/youzi <主题>`。
>
> 本文档**只讲安装**。用法看 [使用进阶.md](使用进阶.md),极简上手看 [快速入门.md](快速入门.md),完全新手看 [小白入门.md](小白入门.md)。

---

## 📑 目录

1. [系统要求](#1-系统要求)
2. [必装 · Skill 本体](#2-必装·skill-本体)
3. [可选 · 7 个爬虫 adapter](#3-可选·7-个爬虫-adapter)
4. [可选 · 商业 API Key](#4-可选·商业-api-key)
5. [验证安装](#5-验证安装)
6. [更新 & 卸载](#6-更新--卸载)
7. [常见安装问题](#7-常见安装问题)

---

## 1. 系统要求

| 工具 | 最低版本 | 检查命令 | 没装怎么办 |
|---|---|---|---|
| **Python** | 3.8+ | `python3 --version` | Mac: `brew install python3` · Win: [python.org](https://python.org) |
| **Node.js** | 18+ | `node --version` | Mac: `brew install node` · Win: [nodejs.org](https://nodejs.org) |
| **Git** | 任意 | `git --version` | Mac: `brew install git` · Win: [git-scm.com](https://git-scm.com) |
| **Claude Code** | v1.0+ | `claude --version` | [claude.com/download](https://claude.com/download) |
| **网络** | 出口可达 github.com / claude.ai | 浏览器试一下 | 公司内网找 IT 代理 |

**磁盘占用**:约 50 MB(skill 本体)+ 各爬虫依赖 200-800 MB 不等。

---

## 2. 必装 · Skill 本体

**目标**:让 Claude Code 能识别 `/youzi <主题>` 命令。

### 2.1 方式 A · 软链接(推荐 · 改源码即时生效)

```bash
# 把 youzi 项目放在 ~/youzi(或任意位置)
ln -s $(pwd) ~/.claude/skills/youzi

# 验证
ls -la ~/.claude/skills/youzi
# 应该看到 SKILL.md / render.py / adapters/ / templates/ 等文件
```

### 2.2 方式 B · 复制目录

```bash
mkdir -p ~/.claude/skills
cp -R . ~/.claude/skills/youzi
```

### 2.3 方式 C · Git clone(团队共享)

```bash
git clone https://github.com/你的用户名/youzi.git ~/youzi
ln -s ~/youzi ~/.claude/skills/youzi
```

### 验证 Skill 安装

```bash
# 1. 文件存在
test -f ~/.claude/skills/youzi/SKILL.md && echo "✓ SKILL.md OK"

# 2. Claude Code 能识别
# 重启 Claude Code,然后输入 /youzi,看是否有自动补全
```

---

## 3. 可选 · 7 个爬虫 adapter

`/youzi` 内置 7 个爬虫,全部基于 Python,**不装也能跑**(降级到 WebSearch)。但**强烈推荐装至少 firecrawl + crawl4ai** 才能产出 80%+ 数据完整度。

| # | 爬虫 | 类型 | 装法 | 推荐度 |
|---|---|---|---|---|
| 1 | **firecrawl** | 商业 API | CLI: `npm install -g firecrawl-cli` + 配置 API key | ⭐⭐⭐⭐⭐ |
| 2 | **crawl4ai** | 开源 Python | `pip install crawl4ai` + `crawl4ai-setup` | ⭐⭐⭐⭐⭐ |
| 3 | **trafilatura** | 开源 Python | `pip install trafilatura` | ⭐⭐⭐⭐ |
| 4 | **newspaper3k** | 开源 Python | `pip install newspaper3k lxml_html_clean` | ⭐⭐⭐ |
| 5 | **readability-lxml** | 开源 Python | `pip install readability-lxml markdownify` | ⭐⭐⭐ |
| 6 | **markdownify** | 开源 Python | `pip install markdownify beautifulsoup4` | ⭐⭐ |
| 7 | **playwright** | 开源 Python | `pip install playwright && playwright install chromium` | ⭐⭐⭐⭐(需登录时用) |

### 3.1 一键全装(推荐)

```bash
# 开源爬虫 6 个 · 一次性
pip install crawl4ai trafilatura newspaper3k lxml_html_clean readability-lxml markdownify beautifulsoup4 playwright requests
crawl4ai-setup
playwright install chromium

# 商业爬虫 firecrawl(需要 Node.js)
npm install -g firecrawl-cli
firecrawl config -k fc-你的APIkey     # 去 https://firecrawl.dev 注册
```

### 3.2 极简装(只要 firecrawl + crawl4ai)

```bash
pip install crawl4ai
crawl4ai-setup
npm install -g firecrawl-cli
firecrawl config -k fc-你的APIkey
```

### 3.3 零爬虫装(只跑得动,数据少)

不装任何爬虫,降级到 Claude Code 内置 `WebSearch` + `WebFetch`。报告能出但数据密度低。

### 验证爬虫

```bash
python3 -c "
from adapters import list_scrapers
for n, ok in list_scrapers().items():
    print(f'  {\"✓\" if ok else \"✗\"} {n}')
"
```

期望输出:7 行 ✓(或部分 ✓,根据装了多少)。

---

## 4. 可选 · 商业 API Key

`/youzi` 内置的 firecrawl adapter 需要 API key 才能工作。其他 6 个爬虫免费。

### 4.1 Firecrawl(主推)

```bash
# 1. 注册:https://firecrawl.dev (免费 500 页/月)
# 2. 拿 API key(以 fc- 开头)
# 3. 配置
firecrawl config -k fc-你的APIkey

# 或环境变量
export FIRECRAWL_API_KEY=fc-你的APIkey
```

### 4.2 Anthropic(Step 3 LLM 提取用)

`/youzi` 在 Step 3 用 LLM 提取 13 字段结构化数据。如未配 LLM key,可用本地启发式降级(质量略低)。

```bash
export ANTHROPIC_API_KEY=sk-ant-xxx
# 或
export OPENAI_API_KEY=sk-xxx
```

---

## 5. 验证安装

### 5.1 一键验证脚本

```bash
cd ~/.claude/skills/youzi
python3 scripts/render_smoke.py
```

期望输出:
```
✓ Rendered: /tmp/pre-commit-render-smoke.html
  · 6 competitors
  · 6 opportunities

=== self-check ===
  ✓ 竞品 ≥ 3
  ...
```

### 5.2 跑真实主题

```bash
# 在 Claude Code 里输入
/youzi 你的主题
```

5-15 分钟后报告应自动生成在 `~/youzi-out/<主题>-<日期>/report.html`。

### 5.3 全功能验证

```bash
python3 scripts/run_youzi.py \
  --topic "测试主题" \
  --analysis examples/whatsapp-advertising-demo.json \
  --output /tmp/test-report.html
```

期望:看到 `✓ Rendered: /tmp/test-report.html` + `=== self-check ===` 全过。

---

## 6. 更新 & 卸载

### 6.1 更新 skill

```bash
# 方式 A:git pull
cd ~/youzi && git pull

# 方式 B:重新复制
cp -R . ~/.claude/skills/youzi

# 软链接方式无需任何操作(链接自动反映源变化)
```

### 6.2 更新爬虫

```bash
pip install --upgrade crawl4ai trafilatura newspaper3k readability-lxml markdownify playwright
playwright install chromium
npm update -g firecrawl-cli
```

### 6.3 完全卸载

```bash
# 1. 移除 skill
rm ~/.claude/skills/youzi
# 或 (软链接): rm $(readlink ~/.claude/skills/youzi) ~/.claude/skills/youzi

# 2. 移除爬虫(可选)
pip uninstall crawl4ai trafilatura newspaper3k readability-lxml markdownify playwright
npm uninstall -g firecrawl-cli

# 3. 清理报告缓存
rm -rf ~/youzi-out/
```

---

## 7. 常见安装问题

### Q1: `claude: command not found`
Claude Code 没装。去 [claude.com/download](https://claude.com/download)。

### Q2: `python3 --version` 报版本 < 3.8
升级 Python:
- Mac: `brew install python3` 或 [python.org](https://python.org) 下载
- Linux: `sudo apt install python3.10` 或编译

### Q3: `pip install crawl4ai` 报 Microsoft C++ Build Tools 缺失
Windows 用户:`pip install crawl4ai` 需要先装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)。
Mac/Linux:通常没问题,如有装 Xcode CLT: `xcode-select --install`。

### Q4: `playwright install chromium` 下载很慢/失败
中国/受限网络用户:
```bash
# 用国内镜像
export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright
playwright install chromium
```

### Q5: `firecrawl config -k ...` 后还是报 API key 缺失
CLI 配置只在当前 session 生效。永久生效:
```bash
echo 'export FIRECRAWL_API_KEY=fc-xxx' >> ~/.zshrc   # 或 ~/.bashrc
source ~/.zshrc
```

### Q6: skill 装好了但 Claude Code 不识别
1. **完全退出 Claude Code**(Cmd+Q)再重开
2. 检查 `~/.claude/skills/youzi/SKILL.md` 文件存在
3. 输入 `/youzi` 时,斜杠后应有自动补全

### Q7: 中文用户跑出乱码或字体问题
浏览器需要装 Noto Sans CJK 字体:
- Mac: 已自带
- Win: Win10+ 已自带
- Linux: `sudo apt install fonts-noto-cjk`

---

## ✅ 安装完成的标志

✅ `python3 scripts/render_smoke.py` 输出全绿
✅ `/youzi <主题>` 在 Claude Code 里能跑出报告
✅ 报告 HTML 浏览器打开有完整 9 个章节 + Sources 区块
✅ `python3 -c "from adapters import list_scrapers"` 至少 3 个 ✓

**下一步**:看 [使用进阶.md](使用进阶.md) 学会高级配置。

---

## 📚 相关文档

- [README.md](README.md) — 项目总览
- [快速入门.md](快速入门.md) — 5 分钟跑通
- [小白入门.md](小白入门.md) — 完全新手版
- [使用进阶.md](使用进阶.md) — 使用手册 + 进阶技巧
- [SKILL.md](SKILL.md) — Claude 触发入口(给 Claude 看)
- [references/crawl-strategy.md](references/crawl-strategy.md) — 爬虫策略详解
