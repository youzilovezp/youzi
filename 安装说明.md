# 📦 /youzi · 安装说明

> 只讲安装 / 更新 / 卸载,全程约 5 分钟。怎么用看 [📖 使用手册.md](使用手册.md),项目总览看 [👋 README.md](README.md)。

## 1. 先装好这几样

| 工具 | 最低版本 | 检查命令 | 没装的怎么办 |
|---|---|---|---|
| 🐍 Python | 3.8+ | `python3 --version` | [python.org](https://python.org) |
| 🟢 Node.js | 18+ | `node --version` | [nodejs.org](https://nodejs.org) |
| 🌿 Git | 任意 | `git --version` | Mac 自带 · Win: [git-scm.com](https://git-scm.com) |
| 🤖 Claude Code | v1.0+ | `claude --version` | [claude.com/download](https://claude.com/download) |

> 💡 `/youzi` 内置 13 个爬虫,**不装也能跑**(降级到 WebSearch),但推荐至少装 firecrawl + crawl4ai,覆盖率从 50% 提升到 99%。
>
> ⚠️ **目前支持 macOS / Linux**。Windows 用户请先用 [WSL](https://learn.microsoft.com/windows/wsl/install)。

## 2. 安装 · Skill 本体

**Claude Code：**

```bash
# 软链接(推荐 · 改源码即时生效)
ln -s $(pwd) ~/.claude/skills/youzi

# 或复制目录
mkdir -p ~/.claude/skills && cp -R . ~/.claude/skills/youzi

# 验证
test -f ~/.claude/skills/youzi/SKILL.md && echo "✓ SKILL.md OK"
```

> ⚠️ **完全退出 Claude Code 再重开**(Mac: Cmd+Q / Win: Alt+F4),否则识别不到新 skill。

**opencode：**

```bash
mkdir -p ~/.config/opencode/skills
ln -s $(pwd) ~/.config/opencode/skills/youzi

# 验证
test -f ~/.config/opencode/skills/youzi/SKILL.md && echo "✓ opencode SKILL.md OK"
```

> ⚠️ 重开 opencode 会话后,`/youzi` 出现在可用 skills 列表即集成成功。SKILL.md 内的命令路径 `<skill-root>` 按安装根目录替换(Claude `~/.claude/skills/youzi` · opencode `~/.config/opencode/skills/youzi`);两处同时安装共享同一源码目录,改动即时生效。

## 3. 安装 · 13 个爬虫(强烈推荐)

`/youzi` 内置 13 个爬虫,推荐至少装 firecrawl + crawl4ai。

### 一键全装(推荐)

```bash
pip install jinja2 crawl4ai crawlee camoufox trafilatura newspaper3k lxml_html_clean readability-lxml markdownify html2text playwright scrapy beautifulsoup4 requests-html requests
crawl4ai-setup
camoufox fetch
playwright install chromium

npm install -g firecrawl-cli
firecrawl config -k fc-你的APIkey     # https://firecrawl.dev 注册
```

### 极简装(只要 firecrawl + crawl4ai)

```bash
pip install crawl4ai && crawl4ai-setup
npm install -g firecrawl-cli && firecrawl config -k fc-你的APIkey
```

### 验证

```bash
python3 -c "from adapters import list_scrapers; [print(f'  {\"✓\" if ok else \"✗\"} {n}') for n, ok in list_scrapers().items()]"
```

期望输出:13 行 ✓(部分 ✓ 也可,按装的算)。

## 4. 商业 API Key(可选)

| 爬虫 | 怎么拿 key | 配置命令 |
|---|---|---|
| **firecrawl**(主推) | [firecrawl.dev](https://firecrawl.dev) 注册(免费 500 页/月) | `firecrawl config -k fc-你的key` |
| **jina** | 免费 20 req/min,无需 key | `pip install jina` |
| **Anthropic**(Step 3 用) | [console.anthropic.com](https://console.anthropic.com) | `export ANTHROPIC_API_KEY=sk-ant-xxx` |

## 5. 验证安装

```bash
cd ~/.claude/skills/youzi
python3 scripts/render_smoke.py
```

期望输出:`✓ Rendered: /tmp/pre-commit-render-smoke.html` + `=== self-check ===` 全过。

也可以打开 Claude Code 输入 `/youzi`,出现自动补全 = 成功。

## 6. 更新

```bash
cd ~/youzi && git pull    # 更新 skill 源码
pip install --upgrade crawl4ai crawlee camoufox trafilatura newspaper3k readability-lxml markdownify html2text playwright scrapy requests-html
npm update -g firecrawl-cli
```

重启 Claude Code 生效。

## 7. 卸载

```bash
# 1. 移除 skill
rm ~/.claude/skills/youzi

# 2. 移除爬虫(可选)
pip uninstall crawl4ai crawlee camoufox trafilatura newspaper3k readability-lxml markdownify html2text playwright scrapy requests-html
npm uninstall -g firecrawl-cli

# 3. 清理报告缓存
rm -rf ~/youzi-out/
```

## 8. 常见问题

| 问题 | 解决 |
|---|---|
| `python: command not found` | 用 `python3`(Mac 自带);仍没有就按上表装 |
| `node: command not found` | [nodejs.org](https://nodejs.org) 下载安装 |
| `claude: command not found` | [claude.com/download](https://claude.com/download) |
| `pip install crawl4ai` 报 C++ Build Tools 缺失 | Win: 装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/);Mac: `xcode-select --install` |
| `playwright install chromium` 下载很慢 | `export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright` |
| `firecrawl config -k ...` 后还是报 API key 缺失 | 永久生效:`echo 'export FIRECRAWL_API_KEY=fc-xxx' >> ~/.zshrc && source ~/.zshrc` |
| 装完不识别 `/youzi` | 完全退出 Claude Code 再重开;还不行重跑安装 |
| 中文乱码 / 字体问题 | 装 [Noto Sans CJK](https://fonts.google.com/noto/specimen/Noto+Sans+SC);Mac/Win10+ 已自带;Linux: `sudo apt install fonts-noto-cjk` |

---

**下一步**:打开 Claude Code 输入 `/youzi 在线协作工具`,跟着 [📖 使用手册.md](使用手册.md) 走。
