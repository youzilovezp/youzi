# /youzi · 使用手册

> **`/youzi <主题>` → 自动挖顶尖竞品 → 深度分析 → 颠覆性机会清单 → 精美 HTML 报告**
>
> 装环境看 [📦 安装说明.md](安装说明.md),项目总览看 [👋 README.md](README.md)。

---

## 📑 目录

**A. 上手**(新手必读)
- [1. 5 分钟跑通](#1-5-分钟跑通)
- [2. 详细安装](#2-详细安装)
- [3. 第一次使用](#3-第一次使用)

**B. 使用手册**(老用户)
- [4. 基础用法](#4-基础用法)
- [5. 完整参数](#5-完整参数)
- [6. 输出位置](#6-输出位置)

**C. 进阶**
- [7. 重渲染/批量分析](#7-重渲染批量分析)
- [8. 进阶配置](#8-进阶配置)

**D. 排错与 FAQ**
- [9. 常见问题 FAQ](#9-常见问题-faq)
- [10. 故障排查](#10-故障排查)

---

# A. 上手

## 1. 5 分钟跑通

如果你只想立刻用,跳过所有细节,看这里:

```bash
# 1. 安装 skill(一次性)
ln -s $(pwd) ~/.claude/skills/youzi

# 2. 打开 Claude Code,输入:
/youzi 在线协作工具

# 3. 等 5-15 分钟,报告自动生成,浏览器自动打开
```

报告长这样:

```
┌─────────────────────────────────────┐
│ 🎯 [主题] · 竞争格局与机会图谱        │
├─────────────────────────────────────┤
│ 01 背景与目标                          │
│ 02 结论与建议(SWOT + Top 3 机会)        │
│ 03 产品定位分析                        │
│ 04 商业策略分析(6 维评分 + 定价)         │
│ 05 产品设计(117 项功能 · 厂商对比矩阵)  │
│ 06 产品数据分析                        │
│ 07 用户反馈分析                        │
│ 08 其他竞品资料库(15 家市场全景)        │
│ ★ 来源与参考资料(168 条带 URL 证据)    │
└─────────────────────────────────────┘
```

> **小提示**:`youzi` 装了 7 个爬虫工具**并行工作**,覆盖率从 50% 提升到 99%。推荐至少装 firecrawl + crawl4ai,详细看 [📦 安装说明](安装说明.md)。

---

## 2. 详细安装

### 2.1 前置环境

| 工具 | 最低版本 | 检查命令 | 没装怎么办 |
|---|---|---|---|
| **Python** | 3.8+ | `python3 --version` | Mac: `brew install python3` · Win: [python.org](https://python.org) |
| **Node.js** | 18+ | `node --version` | Mac: `brew install node` · Win: [nodejs.org](https://nodejs.org) |
| **Git** | 任意 | `git --version` | Mac: `brew install git` · Win: [git-scm.com](https://git-scm.com) |
| **Claude Code** | v1.0+ | `claude --version` | [claude.com/download](https://claude.com/download) |

### 2.2 装 skill 本体

**软链接(推荐 · 改源码即时生效)**:
```bash
ln -s $(pwd) ~/.claude/skills/youzi
```

**复制目录**:
```bash
mkdir -p ~/.claude/skills
cp -R . ~/.claude/skills/youzi
```

**验证**:
```bash
test -f ~/.claude/skills/youzi/SKILL.md && echo "✓ SKILL.md OK"
```

### 2.3 装爬虫(强烈推荐)

`/youzi` 内置 7 个爬虫,**不装也能跑**(降级到 WebSearch),但推荐至少装 firecrawl + crawl4ai:

| # | 爬虫 | 类型 | 装法 | 推荐度 |
|---|---|---|---|---|
| 1 | **firecrawl** | 商业 API | `npm install -g firecrawl-cli` + 配置 API key | ⭐⭐⭐⭐⭐ |
| 2 | **crawl4ai** | 开源 Python | `pip install crawl4ai && crawl4ai-setup` | ⭐⭐⭐⭐⭐ |
| 3 | **trafilatura** | 开源 Python | `pip install trafilatura` | ⭐⭐⭐⭐ |
| 4 | **newspaper3k** | 开源 Python | `pip install newspaper3k lxml_html_clean` | ⭐⭐⭐ |
| 5 | **readability-lxml** | 开源 Python | `pip install readability-lxml markdownify` | ⭐⭐⭐ |
| 6 | **markdownify** | 开源 Python | `pip install markdownify beautifulsoup4` | ⭐⭐ |
| 7 | **playwright** | 开源 Python | `pip install playwright && playwright install chromium` | ⭐⭐⭐⭐(需登录时用) |

**一键全装**:
```bash
# 开源 6 个
pip install crawl4ai trafilatura newspaper3k lxml_html_clean readability-lxml markdownify beautifulsoup4 playwright requests
crawl4ai-setup
playwright install chromium

# 商业 firecrawl(需要 Node.js)
npm install -g firecrawl-cli
firecrawl config -k fc-你的APIkey     # https://firecrawl.dev 注册
```

**验证**:
```bash
python3 -c "from adapters import list_scrapers; [print(f'  {\"✓\" if ok else \"✗\"} {n}') for n, ok in list_scrapers().items()]"
```

期望输出:7 行 ✓(部分 ✓ 也可,按装的算)。

### 2.4 注册 firecrawl(主推爬虫)

1. 浏览器打开 [firecrawl.dev](https://firecrawl.dev) → Sign Up(可 Google 一键登录)
2. 登录后左侧菜单点 `API Keys` → `Create API Key` → 命名(如 `youzi-local`)→ 复制保存
3. 配置:`firecrawl config -k fc-你的key`

---

## 3. 第一次使用

打开 Claude Code,输入:

```
/youzi <你想做的主题>
```

**示例**:
```
/youzi 在线协作工具
/youzi AI 编程助手
/youzi 短视频剪辑软件
/youzi 跨境电商
```

**5-15 分钟后会看到**:
```
⏳ 正在搜索 6 个搜索引擎...
⏳ 正在爬取 6 个竞品官网...
⏳ 正在分析 13 个维度...
⏳ 正在生成 HTML 报告...
✅ 报告已生成!
📂 ~/youzi-out/<主题>-<日期>/report.html
```

输入 `y` 回车,浏览器自动打开报告。

> **看不到 `/youzi` 命令**:完全退出 Claude Code(Cmd+Q)再重开。

---

# B. 使用手册

## 4. 基础用法

```
/youzi <主题>
```

**示例**:
```
/youzi 在线协作工具
/youzi AI 编程助手
/youzi 视频剪辑软件
/youzi 团队 IM 工具
/youzi 跨境电商平台
```

## 5. 完整参数

```
/youzi <主题> [选项]

选项:
  --depth shallow|deep        # 爬取深度(默认 shallow)
  --count N                    # 竞品数量(默认 9)
  --region cn|us|global        # 限定区域(默认 global)
  --focus "文本"               # 分析焦点(如 "中小企业 / 移动端")
  --include "Slack,钉钉"       # 强制包含指定竞品
  --exclude "Worktile"         # 排除指定竞品
  --refresh                    # 强制重新爬取(忽略缓存)
  --no-viz                     # 只输出 JSON,不渲染 HTML
```

**示例**:
```bash
# 深度分析 + 12 个竞品
/youzi AI 编程助手 --depth deep --count 12

# 限定中国市场 + 移动端焦点
/youzi 短视频工具 --region cn --focus "移动端 抖音生态"

/youzi 跨境电商 --include "Shopee,Temu,SHEIN"

/youzi 低代码平台 --exclude "OutSystems"

/youzi AI 写作 --depth deep --refresh
```

## 6. 输出位置

默认输出到:
```
~/youzi-out/<主题>-<日期>/
├── 01-competitors-list.json   # 竞品候选清单
├── 02-raw/                    # 原始爬取数据(可缓存复用)
│   ├── <name>.md              # 每个竞品的网页内容
│   ├── <name>-visual.md       # 视觉分析(可选)
│   ├── <name>-code.md         # 代码仓库分析(可选)
│   └── <name>-social.md       # 社交信号(可选)
├── 03-analysis.json           # 结构化分析结果(13 字段 × N 竞品)
└── report.html                # ⭐ 最终报告(单文件 ~300KB)
```

**直接打开报告**:
```bash
# macOS
open ~/youzi-out/<主题>-<日期>/report.html

# Linux
xdg-open ~/youzi-out/<主题>-<日期>/report.html

# Windows
start ~/youzi-out/<主题>-<日期>/report.html
```

### 报告 9 大模块

```
1. 背景与目标        - 分析背景 + 5 个目标 + 信息条
2. 结论与建议        - TL;DR + 同类厂商聚类 + SWOT 全图 + Top 3 机会
3. 产品定位分析      - 6 家 slogan + 用户定位表 + 目标用户重叠
4. 商业策略分析      - 6 维评分 + 成立时间线 + 阶段分布 + 定价明细
5. 产品设计分析      - 4 端支持 + 117 项功能厂商对比矩阵 + 视觉 + 技术栈
6. 产品数据分析      - 7 个市场细分 + 整体增长 + 关键增长点
7. 用户反馈分析      - 6 家正/负反馈 + 总结
8. 其他竞品资料库    - 15 家市场全景玩家
★ 来源与参考资料    - 168 条带 URL 证据(分 10 类)
```

**报告交互能力**:
- 🌗 右上角按钮 — 切换浅色 / 深色 / 跟随系统
- 📑 右侧浮动 TOC — 锚点跳转
- 🖨️ Ctrl+P / Cmd+P — 打印(CSS 优化过)
- 🔗 任何结论后的 [N] — 点击跳转到对应证据

---

# C. 进阶

## 7. 重渲染/批量分析

### 7.1 重渲染(不改数据)

如果只想改报告样式 / 加新维度,**不用重新爬**:

```bash
# 跳过爬取,直接用已有 JSON 重渲染
python3 ~/.claude/skills/youzi/scripts/render_v2.py \
  --input ~/youzi-out/AI编程助手-2026-08-15/03-analysis.json \
  --output ~/youzi-out/AI编程助手-2026-08-15/report-v2.html
```

### 7.2 批量分析多个主题

```bash
#!/bin/bash
# batch-analyze.sh
for topic in "在线协作工具" "AI 编程助手" "视频剪辑工具"; do
  echo "=== Analyzing: $topic ==="
  # 在 Claude Code 里调一次 /youzi $topic
done
```

### 7.3 与其他工具组合

```bash
# 跑完后,把 JSON 转给 Notion
cat ~/youzi-out/<topic>/03-analysis.json | jq '.opportunities' | \
  curl -X POST https://api.notion.com/v1/pages -H "Authorization: Bearer $NOTION_TOKEN"

# 把 HTML 报告上传到 Confluence
curl -X POST -u user:pass \
  --data-binary @~/youzi-out/<topic>/report.html \
  https://your-confluence/wiki/rest/api/content
```

### 7.4 导出 JSON 数据

```bash
cat ~/youzi-out/<topic>/03-analysis.json | \
  python3 -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d['competitors'], ensure_ascii=False, indent=2))"
```

### 7.5 在 CI/CD 里定期跑

```yaml
# .github/workflows/weekly-competitive.yml
name: Weekly Competitive Analysis
on:
  schedule: [{cron: '0 9 * * MON'}]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run /youzi
        run: |
          claude code --skill youzi --prompt "分析:跨境电商 SaaS"
```

## 8. 进阶配置

### 8.1 修改报告主题

报告主题(浅色/深色/跟随系统)可点击右上角 🌗 按钮切换,选择会记住到 localStorage。

### 8.2 修改默认竞品数量

编辑 `~/.claude/skills/youzi/SKILL.md`,找到 `--count N`(默认 9),改成你想要的数字。

### 8.3 添加自定义评分维度

编辑 `~/.claude/skills/youzi/render.py`:
```python
# 找到 SCORE_DIMS,添加新维度
SCORE_DIMS = [
    ("feature_richness", "功能丰富度"),
    ("ux",               "用户体验"),
    ("pricing_value",    "定价性价比"),
    ("integration",      "生态集成"),
    ("ai_capability",    "AI 能力"),
    ("momentum",         "增长势头"),
    ("security",         "安全合规"),   # ← 你加的
]
```

然后在数据 JSON 里加 `"scores": { ..., "security": 8 }`。

### 8.4 修改配色

编辑 `~/.claude/skills/youzi/templates/report.html`,找到 `:root` 里的 `--accent` 等变量。

---

# D. 排错与 FAQ

## 9. 常见问题 FAQ

### Q1:跑一次要多久?

| 模式 | 时间 |
|---|---|
| shallow + 9 竞品(默认) | 3-5 分钟 |
| deep + 12 竞品 | 8-15 分钟 |
| shallow + 3 竞品 | 1-2 分钟 |

时间主要花在 WebFetch / firecrawl 爬网页 + LLM 分析。

### Q2:报告是单文件吗?能离线看吗?

**是的。** 报告 ~300KB 单文件,CSS/JS/SVG 全部 inline,**零外部依赖**。可以邮件发送、上传网盘、放进 USB,断网也能看。

### Q3:能分析中文竞品吗?

**可以。** Skill 默认就是中文友好:
- 字体栈优先 PingFang SC / 微软雅黑
- 搜索引擎自动用中文查询

### Q4:数据是实时抓的吗?会不会过时?

**每次跑都是实时抓的。** 建议:
- 高频赛道(AI、加密)每月刷一次
- 稳定赛道(SaaS、电商)每季度刷一次

`--refresh` 强制重爬;不加则用 `02-raw/` 缓存。

### Q5:能跑私有数据吗?会上传到云吗?

**不会。** skill 本体只调本地工具(WebSearch / WebFetch / MCP)。所有分析在本地 Claude 里完成,HTML 写到本地磁盘。

### Q6:生成的报告能改吗?

**能。** 三种方式:
1. **手动改 HTML** — 直接编辑 `report.html`
2. **改模板** — 编辑 `templates/report.html`,重跑 `render.py`
3. **改数据** — 编辑 `03-analysis.json`,重跑 `render.py`

### Q7:竞品数量有上限吗?

**理论无上限**,但建议 5-15 个。
- 太少(< 5):覆盖不全,颠覆性机会少
- 太多(> 20):LLM 分析质量下降,报告冗长

### Q8:怎么保证分析质量?

skill 强制要求 **每条 strengths / weaknesses 必须带 URL 证据**(来自抓到的网页 / 评测)。如果证据不足,会标"无足够证据",不瞎编。

### Q9:报告里所有 URL 都能访问吗?

`/youzi` 工程对每条来源都做了 URL 验证 — 0 个真 404。G2/Reddit/Crunchbase/LinkedIn 这类反爬站点标记为"人需登录"但人访问正常。

## 10. 故障排查

### 问题 1:`/youzi` 命令不识别

**症状**:在 Claude Code 输入 `/youzi XXX`,提示"未找到命令"。

**排查**:
```bash
# 1. 检查 skill 目录是否存在
ls -la ~/.claude/skills/youzi/SKILL.md

# 2. 如果不存在,重新安装
ln -s ~/youzi ~/.claude/skills/youzi

# 3. 完全退出 Claude Code(Cmd+Q)再重开
```

### 问题 2:报告生成但内容空白

**症状**:HTML 生成成功(300KB),但卡片里 strengths / weaknesses 是空的。

**原因**:通常是 WebFetch 抓到的网页是 JS 重度渲染(SPA),只拿到空 HTML。

**解决**:
```bash
# 装 firecrawl(处理 96% SPA)
npm install -g firecrawl-cli
firecrawl config -k fc-你的key
```

### 问题 3:爬取超时

**症状**:`02-raw/` 里某些竞品的 md 文件大小为 0。

**解决**:
```bash
# 1. 重跑(自动重试失败的)
/youzi <主题> --refresh

# 2. 如果还是失败,单独跑深度模式
/youzi <主题> --depth deep

# 3. 终极方案:手动指定竞品
/youzi <主题> --include "已确认能爬到的竞品"
```

### 问题 4:firecrawl 报 API key 错

```bash
# 去 https://firecrawl.dev 免费注册
# 拿到 API key 后:
firecrawl config -k fc-xxxxx
```

### 问题 5:报告样式错乱

```bash
# 1. 清浏览器缓存
# 2. 检查 HTML 完整性
wc -c ~/youzi-out/<topic>/report.html
# 应该 ~300KB

# 3. 如果 < 50KB,说明渲染器出问题
python3 scripts/render_v2.py \
  --input ~/youzi-out/<topic>/03-analysis.json \
  --output /tmp/test.html
# 看错误信息
```

### 问题 6:磁盘被报告占满

```bash
# 清理 30 天前的旧报告
find ~/youzi-out/ -maxdepth 1 -type d -mtime +30 -exec rm -rf {} \;

# 或一次性全删
rm -rf ~/youzi-out/
```

### 问题 7:中文乱码

```bash
# 1. 确保 terminal locale 是 UTF-8
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8

# 2. 确保 HTML 文件本身是 UTF-8(默认就是)
file ~/youzi-out/<topic>/report.html
# 应该输出 "UTF-8 Unicode text"

# 3. 浏览器强制 UTF-8:Chrome DevTools → Network → 勾选 "Disable cache"
```

### 问题 8:某些 URL 打开 404

`/youzi` 工程已自动验证所有引用 URL。0 个真 404。如发现:
- 真 404 → 工程已修复
- 403 anti-bot → 标记"人需登录",G2/Reddit/Crunchbase/LinkedIn 人访问正常
- 超时 → 重新跑 /youzi <主题> --refresh

---

## 📞 获取帮助

| 渠道 | 链接 |
|---|---|
| 项目地址 | `~/youzi` |
| 示例数据 | `examples/whatsapp-advertising-demo.json` |
| 爬虫总结工具 | `python3 scripts/crawl_summarize.py` |
| Smoke test | `python3 scripts/render_smoke.py` |
| 13 字段提取框架 | `references/analysis-framework.md` |
| 爬取策略详解 | `references/crawl-strategy.md` |

---

**版本**:v3.0 · 2026-08-24 · 由 `/youzi` skill 自动生成
