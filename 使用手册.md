# 🍊 /youzi · 使用手册

> **`/youzi <主题>` → 自动挖顶尖竞品 → 深度分析 → 颠覆性机会清单 → 精美 HTML 报告**
>
> 安装请先看 [📦 安装说明.md](安装说明.md)。

---

## 目录

| § | 章节 | 什么时候看 |
| --- | --- | --- |
| 1 | [跑第一次分析](#1-跑第一次分析) | 刚装好，第一次用 |
| 2 | [看懂报告](#2-看懂报告) | 报告生成了，想知道怎么看、能信多少 |
| 3 | [命令速查](#3-命令速查) | 忘了某个参数 |
| 4 | [输出与缓存](#4-输出与缓存) | 想重渲染 / 复用数据 / 管理磁盘 |
| 5 | [进阶玩法](#5-进阶玩法) | 想无头跑 / CI 定期跑 / 改评分维度 / 改配色 |
| 6 | [常见问题](#6-常见问题) | 出错了 |

---

## 1. 跑第一次分析

### 1.1 打开 AI 工具

打开 Claude Code / opencode / Codex / EasyCode 中任意一个。

### 1.2 输入命令

在对话框输入（主题换成你想调研的赛道）：

```
/youzi 在线协作工具
```

其他主题示例：

```
/youzi AI 编程助手
/youzi 短视频剪辑软件
/youzi 跨境电商平台
```

### 1.3 中途会看到什么

AI 会按 7 步工作流执行，关键节点会回显进度：

```
/youzi 在线协作工具 → count=9, depth=shallow, region=global, out=~/youzi-out/在线协作工具-2026-08-27/
Found 10 competitors: Notion, Lark, Coda, ...
[✓ notion] 45.2s | 6 pages | 不充分:无 | failures: 0
[✓ lark]   38.7s | 6 pages | 不充分:pricing | failures: 2
...
✓ Report: ~/youzi-out/在线协作工具-2026-08-27/report.html
```

- 第 1 行：解析出的参数 + 输出目录
- `Found N competitors`：搜索发现的竞品名单
- `[✓ 竞品名]`：每个竞品的取证结果（页数 / 哪些信息不充分 / 失败数）
- 最后一行：报告路径

### 1.4 等待与打开

一般 **5-15 分钟**（每竞品默认预算 5 分钟，超时会诚实标注「未验证」而不是硬编）。完成后 AI 会给出一段总结并提示打开报告：

```bash
open ~/youzi-out/<主题>-<日期>/report.html       # macOS
xdg-open ~/youzi-out/<主题>-<日期>/report.html   # Linux
```

> 💡 想换主题 / 加竞品 / 调整 focus？直接说就行——已抓的数据缓存在 `02-raw/`，重跑很快。

---

## 2. 看懂报告

### 2.1 九大章节

| § | 章节 | 实际内容 |
| --- | --- | --- |
| 01 | 背景与目标 | 分析背景 + 目标 + 关键数字信息条（竞品数/功能数/机会数/来源数）+ 分析方法说明 + 阅读建议 |
| 02 | 结论与建议 | 2.1 专业术语速查 → 2.2 启发点 / 2.3 机会点（按竞品分组）→ 2.5 Top 3 颠覆性机会（按 disrupt_score 排序） |
| 03 | 产品定位分析 | 3.1 宣传口号卡片 + 3.2 用户及市场定位表（区域/规模/核心市场/成立年份）+ 3.3 目标用户重叠矩阵 |
| 04 | 定价分析 | 4.1 定价明细：每家定价卡（月付/年付分栏、免费档、定制档、年付省%）+ 可折叠证据链（定价页/抓取时间/交叉验证引擎） |
| 05 | 产品设计分析 | 5.1 四端支持表（网页/桌面/移动/其他）+ 5.2 功能对比矩阵（独家标注）+ 5.3 技术栈信号聚类 + 5.4 各家技术栈明细（带原文引用） |
| 06 | 产品数据分析 | 6.0 市场细分全景 + 6.1 迭代信号 + 6.2 发布密度对比 + 6.3 近期发布时间线（无数据时整块诚实占位） |
| 07 | 用户反馈分析 | 每家一张反馈卡：正面 / 负面分栏，每条带 ↗ 来源链接；没抓到就明说「不编造」 |
| 08 | 其他竞品资料库 | 深度分析之外的市场全景玩家，按类别分组；无数据时整节隐藏 |
| ★ | 来源与参考资料 | 全部带 URL 的证据清单（`source_count` 条），零来源时显示红色警告横幅 |

### 2.2 交互能力

- 🌗 右上角 — 切换浅色 / 深色 / 跟随系统（选择会记住）
- 📑 浮动目录 — 章节锚点跳转，一键返回顶部
- 🔗 结论后的 `[N]` 角标 — 跳转到对应证据的原文出处
- 🖨️ Ctrl+P / Cmd+P — 打印（CSS 已优化）
- 📱 响应式 — 桌面 / 平板 / 手机都能看

### 2.3 能信多少？（验证体系）

报告里的每条结论都经过**双门禁**才交付：

| 你看到的 | 含义 |
| --- | --- |
| 定价无徽章 | ≥2 个独立爬虫引擎看到相同价格，**已验证** |
| ⚠ 未验证徽章 | 只有 1 个引擎看到，方向对但建议去官网核对 |
| 字段写「未验证」 | 证据不足，**诚实标注**，绝不用编造数据凑数 |
| `[N]` 角标 | 每条结论可跳回带 URL 的原文证据 |

门禁本身：`render.py` 渲染自检（未解析模板标签 / 数据完整性 / 文件大小 < 1.5MB）+ `verify.py` 离线核验 G1-G8（G1 来源可回溯 / G2 quote 回查 / G3 定价完整性 / G4 缺失诚实 / G5 反伪造 / G6 URL 卫生 / G7 溯源权威性 / G8 结构契约），不过 = 不交付。

---

## 3. 命令速查

### 3.1 基础用法

```
/youzi <主题>
```

### 3.2 完整参数

```
/youzi <主题> [选项]
/youzi <03-analysis.json 路径>        # 用已有数据重渲染，不重爬

选项：
  --depth shallow|deep        # 爬取深度（默认 shallow；deep 每个竞品抓更多页）
  --count N                    # 竞品数量（默认 9）
  --region cn|us|global        # 限定区域（默认 global）
  --focus "文本"               # 分析焦点（如 "中小企业 / 移动端"）
  --include "Slack,钉钉"       # 强制包含指定竞品（插到名单最前）
  --exclude "Worktile"         # 排除指定竞品
```

### 3.3 常用组合

```
/youzi AI 编程助手 --depth deep --count 12          # 深度 + 12 个竞品
/youzi 短视频工具 --region cn --focus "移动端"       # 中国市场 + 移动端焦点
/youzi 跨境电商 --include "Shopee,Temu,SHEIN"       # 指定必须分析的竞品
/youzi 低代码平台 --exclude "OutSystems"            # 排除不想看的
```

---

## 4. 输出与缓存

### 4.1 输出目录结构

默认输出到 `~/youzi-out/<主题>-<日期>/`：

```
~/youzi-out/<主题>-<日期>/
├── 01-competitors-list.json   # 竞品候选清单
├── 02-raw/                    # 原始爬取数据（缓存，可复用）
│   ├── <name>.md              # 每个竞品的网页内容（合并视图）
│   └── <name>.engines.json    # 每引擎独立原文（交叉验证数据源）
├── claims-manifest.json       # 取证台账（URL × 引擎 × 哈希 × 时间）
├── 03-analysis.json           # 13 字段结构化分析结果
├── verify-report.json         # 证据门禁核验报告
└── report.html                # ⭐ 最终报告（单文件 HTML，自检上限 1.5MB）
```

### 4.2 改数据重渲染（不必重爬）

改了 `03-analysis.json`（比如补充字段、调整评分）后 30 秒重出报告：

```bash
cd ~/youzi    # 源仓库（或任一平台的安装目录）
python3 render.py \
  --input ~/youzi-out/<主题>-<日期>/03-analysis.json \
  --output ~/youzi-out/<主题>-<日期>/report-v2.html
```

也可以直接在 AI 工具里：`/youzi ~/youzi-out/<主题>-<日期>/03-analysis.json`。

### 4.3 缓存规则

- 同一输出目录重跑：`02-raw/` 里已抓的页面自动跳过，只补缺的
- 定价证据超过 14 天会被 G3 门禁判为「陈旧」（`TTL_DAYS = 14`），建议重爬后再验证
- 想**强制重爬**某竞品：删掉 `02-raw/<name>.*` 再跑

### 4.4 跑一次要多久

| 模式 | 时间 |
| --- | --- |
| shallow + 9 竞品（默认） | 5-15 分钟 |
| deep + 12 竞品 | 15 分钟以上 |
| 只重渲染（已有 JSON） | <1 分钟 |

### 4.5 磁盘清理

```bash
# 清理 30 天前的旧报告
find ~/youzi-out/ -maxdepth 1 -type d -mtime +30 -exec rm -rf {} \;
# 或一次性全删
rm -rf ~/youzi-out/
```

---

## 5. 进阶玩法

### 5.0 证据助手（写分析前的加速器）

AI 在 Step 3 写 `03-analysis.json` 前，先用证据助手把引文/锚点/定价票备齐（含结构契约速查，避免渲染后才发现格式错）：

```bash
# 每竞品摘要：G7 锚点分级 + 可读正文 + grep 已验证引文 + 定价票上下文
python3 scripts/evidence.py digest OUT_DIR

# 页内搜引文（返回逐字可 grep 的原文行）
python3 scripts/evidence.py quote OUT_DIR <URL> "<正则>"
```

### 5.1 无头跑 / 批量 / CI 定期跑

**方式 A：AI 工具无头模式**（推荐，完整 7 步工作流）：

```bash
# Claude Code 无头跑一次（其他平台用各自的 headless 入口）
claude -p "/youzi 跨境电商 SaaS"

# 批量：写个循环脚本逐主题调用
```

CI 里每周一 9 点自动刷新（`.github/workflows/weekly-competitive.yml`）：

```yaml
on:
  schedule: [{cron: '0 9 * * MON'}]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: claude -p "/youzi 跨境电商 SaaS"
```

**方式 B：run_youzi.py 管线 runner**（适合已有 03-analysis.json 的重跑 / 纯取证）：

```bash
# 渲染 + 双门禁验证一条龙
python3 scripts/run_youzi.py --topic "在线协作工具" \
  --analysis OUT/03-analysis.json --output OUT/report.html

# 只跑 Step 2 取证（给 CI 预热缓存）
python3 scripts/run_youzi.py --topic "..." --crawl-only \
  --competitors "wati,respond.io" --raw-dir OUT/02-raw
```

### 5.2 证据复核（网络门禁）

离线门禁（G1-G8）每次自动跑；想再抽样核 URL 死链和引文漂移：

```bash
python3 verify.py \
  --analysis OUT/03-analysis.json \
  --manifest OUT/claims-manifest.json \
  --raw-dir OUT/02-raw \
  --network --sample 10
```

### 5.3 加自定义评分维度

编辑 `render.py` 的 `SCORE_DIMS`：

```python
SCORE_DIMS = [
    ("feature_richness", "功能丰富度"),
    ("ux",               "用户体验"),
    ("pricing_value",    "定价性价比"),
    ("integration",      "生态集成"),
    ("ai_capability",    "AI 能力"),
    ("momentum",         "增长势头"),
    ("security",         "安全合规"),   # ← 你加的
]
```

然后在 `03-analysis.json` 的 `scores` 里补 `"security": 8`，重渲染即可。

### 5.4 改报告配色

编辑 `templates/report.html` 里 `:root` 的 `--accent` 等 CSS 变量，重渲染生效（link 安装下改源仓库即时生效）。

### 5.5 功能矩阵的两种模式

- 默认 **vendor 模式**：矩阵行 = 本次实爬到的功能并集，每个 ✓ 都可溯源
- **canonical 参考清单**：`03-analysis.json` 显式设 `feature_canonical.enabled=true` 启用人工整理的标准清单做对照（`?` 刻 = 实爬未命中，不代表厂商不支持）

---

## 6. 常见问题

### 6.1 输 `/youzi` 没反应？

AI 工具没重启。完全退出再打开；还不行跑 `./install.sh status` 检查安装。

### 6.2 生成的报告内容空白 / 数据很少？

目标网站是 JS 重度渲染（SPA），静态抓取拿不到内容。装浏览器引擎：

```bash
pip install playwright && playwright install chromium
```

### 6.3 爬取超时 / 某竞品全灭？

- 直接重跑一次（自动复用缓存、只补失败的）
- 还不行就缩小范围：`/youzi <主题> --include "确认能访问的竞品"`
- 全灭的竞品会自动降级到 Wikipedia / Crunchbase / G2 替代证据源，并如实标注

### 6.4 firecrawl 报 API key 错？

```bash
export FIRECRAWL_API_KEY='fc-xxxxx'   # firecrawl.dev 免费注册
# 永久生效：
echo 'export FIRECRAWL_API_KEY=fc-xxx' >> ~/.zshrc && source ~/.zshrc
```

### 6.5 生成的报告能改吗？

能，三种方式（推荐程度递增）：

1. **改数据** — 编辑 `03-analysis.json`，重跑 `render.py`（数据/样式分离，最干净）
2. **改模板** — 编辑 `templates/report.html`，重跑 `render.py`
3. **手动改 HTML** — 直接编辑 `report.html`（下次重渲染会丢失，不推荐）

### 6.6 竞品数量有上限吗？

理论无上限，建议 5-15 个。太少（<5）覆盖不全、机会洞察少；太多（>20）分析质量下降、报告冗长。

### 6.7 怎么保证分析质量 / 不胡编？

三重机制：每条结论强制带 `{值, source_url, quote}` 证据三元组；verify.py G1-G8 逐条回查（引文必须能在原文 grep 到）；抓不到的字段诚实标「未验证」——历史上因此删掉过内置价格库、模板化差异化等所有「看起来完整实际编造」的来源。

### 6.8 报告里的 URL 都能打开吗？

离线门禁做了 URL 卫生检查；G2 / Reddit / Crunchbase 这类反爬站点可能对人正常、对机器 403。发现死链可跑 `verify.py --network`（5.2 节）复核。

### 6.9 报告样式错乱？

```bash
wc -c ~/youzi-out/<主题>/report.html   # 正常几十 KB 到几百 KB（上限 1.5MB）；明显偏小说明渲染异常
python3 scripts/render_smoke.py         # 跑冒烟测试定位
```

### 6.10 中文乱码？

```bash
export LANG=en_US.UTF-8 && export LC_ALL=en_US.UTF-8
```

浏览器显示问题：Chrome DevTools → Network → 勾选 "Disable cache" 后刷新。

---

## 术语速查

| 术语 | 通俗解释 |
| --- | --- |
| Skill | AI 工具的「插件」，教它一套新本事 |
| 证据三元组 | 每条结论 = 值 + 来源 URL + 原文逐字引文 |
| 台账 | `claims-manifest.json`，记录每条证据是什么时候用哪个引擎抓的 |
| 双门禁 | 渲染自检 + 证据核验两道关卡，都过才交付 |
| 引擎白名单 | 实测能打的 5 个爬虫（playwright / trafilatura / newspaper3k / jina / firecrawl） |
| 交叉验证 | 定价要 ≥2 个独立引擎看到相同数字才算「已验证」 |
| 充分性 | 信息够不够用的判定标准，不够就自动换引擎重爬 |
| OUT_DIR | 输出目录 `~/youzi-out/<主题>-<日期>/` |

---

## 寻求帮助

| 渠道 | 链接 |
| --- | --- |
| 取证入口 | `python3 scripts/fetch.py --competitors "a,b" --out-dir OUT` |
| 渲染冒烟测试 | `python3 scripts/render_smoke.py` |
| 13 字段提取框架 | [references/analysis-framework.md](references/analysis-framework.md) |
| 爬取策略详解 | [references/crawl-strategy.md](references/crawl-strategy.md) |
| AI 完整工作流 | [SKILL.md](SKILL.md) |

---

**版本**：v3.1 · 2026-08-27
